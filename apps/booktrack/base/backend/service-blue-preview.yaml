apiVersion: v1
kind: Service
metadata:
  name: svc-booktrack-backend
  namespace: booktrack
  labels:
    app.kubernetes.io/name: booktrack
    app.kubernetes.io/component: backend
    app.kubernetes.io/environment: dev
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: booktrack
    app.kubernetes.io/component: backend
    # app.kubernetes.io/version: blue
  ports:
  - port: 8000
    targetPort: 8000
    name: http
