apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: bookrack-frontend-hpa
  namespace: booktrack
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: booktrack-frontend
  minReplicas: 1
  maxReplicas: 3
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 80
